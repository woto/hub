<%= render(
      ReactComponent.new(name: 'Bookmark',
                         class: '',
                         props: {
                           is_checked: @is_checked,
                           ext_id: @ext_id,
                           favorites_items_kind: @favorites_items_kind
                         }))
%>