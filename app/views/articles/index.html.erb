<% content_for :breadcrumbs do %>
  <li class="breadcrumb-item">
    <%= link_to t('home'), dashboard_path %>
  </li>
  <li class="breadcrumb-item active">
    <%=link_to t('articles'), articles_path %>
  </li>
<% end %>

<% content_for :title do %>
  <%= t('articles') %>
<% end %>

<div class="col-lg-9">
  <div class="card card-lg">
    <div class="card-body">

      <% @articles.each do |article| %>
        <div class="d-flex mb-3">
          <h2><%== article.meta["title"] %></h2>
          <span class="ml-auto text-muted"><%== l article.meta["date"] %></span>
        </div>
        <div class="news-preview">
          <%== article.preview %>
        </div>
        <%= link_to t(:continue_reading), article_path(date: article.date, title: article.title) %>
        <% unless @articles.last == article %>
          <hr>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

<%= paginate @articles %>
