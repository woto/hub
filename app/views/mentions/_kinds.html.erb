<%# TODO: write article %>
<%= f.input :kinds, as: :check_boxes, collection: [''], checked: ['', true],
            wrapper_html: { class: 'd-none'},
            error: false,
            label: false,
            hint: false %>

<%= f.input :kinds,
            as: :check_boxes,
            collection: Mention::KINDS.map { |kind|
              [render(Mentions::KindIconComponent.new(kind: kind)), kind]
            },
            wrapper: :radio_button_group %>
