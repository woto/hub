<%= render(ReactComponent.new(name: 'MentionsCard',
                              class: '',
                              props: {
                                id: mention._id,
                                url: mention.url,
                                title: mention.title,
                                publishedAt: mention.published_at,
                                topics: mention.topics,
                                image: mention.image.try(:first),
                                entities: mention.entities
                              })) %>