<% seo.title! t("titles.realms") %>

<% content_for :breadcrumbs do %>
  <%= render BreadcrumbComponent.new do |b| %>
    <%= b.root_item title: t('breadcrumbs.homepage'), url: root_path %>
    <%= b.item title: t('breadcrumbs.dashboard'), url: dashboard_path %>
    <%= b.item title: t('breadcrumbs.realms'), url: realms_path %>
    <%= b.item title: t('breadcrumbs.edit_realm'), url: edit_realm_path(@realm) %>
  <% end %>
<% end %>

<%= render 'form', realm: @realm %>
