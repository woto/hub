<div class="list-group bg-white">
  <a href="#" class="list-group-item list-group-item-action active">
    Cras justo odio
  </a>
  <% by_tags_menu['aggregations']['group_by_tag']['buckets'].each do |item| %>
    <% link_value = item['key'] %>
    <%= link_to by_tag_help_index_path(link_value), class: 'list-group-item d-flex list-group-item-action justify-content-between align-items-center' do %>
      <%= link_value %>
      <span class="badge badge-primary badge-pill"><%= item['doc_count'] %></span>
    <% end %>
  <% end %>
</div>
