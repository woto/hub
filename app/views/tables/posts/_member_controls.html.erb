<div class="dropdown">

  <button type="button" class="btn btn-white dropdown-toggle" data-bs-toggle="dropdown">
    <%= row.id %>
  </button>

  <div class="dropdown-menu">

    <%= link_to t('buttons.show'), post_path(row.id), class: 'dropdown-item' %>
    <% post = Post.find(row.id) %>
    <% if policy(post).edit? %>
      <%= link_to t('buttons.edit'), edit_post_path(row.id), class: 'dropdown-item' %>
    <% end %>
    <% if policy(post).destroy? %>
      <%= link_to t('buttons.delete'), post_path(row.id), method: :delete, data: { confirm: t('are_you_sure') }, class: 'dropdown-item' %>
    <% end %>

  </div>
</div>
