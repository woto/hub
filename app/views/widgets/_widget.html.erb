<% if defined?(purpose) %>
  <% opts = { purpose: purpose } %>
<% else %>
  <% opts = {} %>
<% end %>

<figure class="attachment w-100">
  <%= render resolve_widgetable_partial(widget, opts), widget: widget %>
</figure>
