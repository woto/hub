# frozen_string_literal: true

require 'rails_helper'

RSpec.describe Identity, type: :model do
  # let(:auth) { Faker::Omniauth.linkedin }
  # let(:key) { Faker::Number.hexadecimal }

  # describe '.process' do
  #   context 'when no associated user' do
  #     subject { described_class.store(key, auth) }

  #     it { is_expected.to be true }
  #   end

  #   context 'when associated user present' do
  #     subject { described_class.store(key, @idn.auth) }

  #     before do
  #       @idn = create(:identity, :with_user)
  #     end

  #     it { is_expected.to be true }
  #     it { expect(Identity.count).to eq 1 }
  #   end
  # end
end
